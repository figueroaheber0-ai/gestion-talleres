services:
  - type: web
    name: gestion-talleres-backend
    runtime: node
    buildCommand: cd backend && npm install && chmod +x ./node_modules/.bin/prisma && ./node_modules/.bin/prisma generate && npm run build
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres.aotevwniuyjqbpnhpcno:Taller2R_Secure_2026!@aws-1-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
      - key: PORT
        value: 3001
      - key: FRONTEND_ORIGIN
        value: "*"
